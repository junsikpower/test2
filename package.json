{
  "name": "pomodoro-timer-webapp",
  "version": "4.0.0",
  "private": true,
  "description": "브라우저에서 파일 하나로 실행되는 완전 로컬형 뽀모도로 타이머 겸 작업 기록 웹앱 (PRD v4)",
  "license": "UNLICENSED",
  "scripts": {
    "test:dev": "jest tests/dev --ci",
    "test:review": "jest tests/review --ci"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "jest-environment-jsdom": "^30.0.5",
    "jest-junit": "^16.0.0",
    "jsdom": "^26.1.0"
  }
}
